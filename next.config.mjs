// Stack: Next.js 15 | File: next.config.mjs
// "standalone" makes `next build` emit a self-contained server.js — the only thing the image needs to copy.
const nextConfig = { output: "standalone" };
export default nextConfig;
